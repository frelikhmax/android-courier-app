<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Courier" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorButtonNormal">@color/nightBlue4</item>
        <item name="colorBackgroundFloating">@color/nightBlue2</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/nightBlue2</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/nightBlue1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:positiveButtonText">@color/white</item>

        <item name="android:colorPressedHighlight">@color/nightBlue3</item>
    </style>
</resources>